{
  "name": "icons",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "svgr --out-dir react-icons --typescript --template template.js -- icons/*.svg ",
    "postbuild": "barrelsby --delete"
  },
  "devDependencies": {
    "@svgr/cli": "^6.5.1",
    "barrelsby": "^2.5.1"
  }
}
